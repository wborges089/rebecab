

## Adicionar ID ao Botão CTA para Integração com Vturb

### O que será feito
Adicionar o atributo `id="cta-btn"` ao botão "Quero Começar Agora" nas duas páginas de vendas.

### Arquivos editados
- `src/pages/Index.tsx` — adicionar `id="cta-btn"` no `<Button>` do CTA
- `src/pages/IndexB.tsx` — adicionar `id="cta-btn"` no `<Button>` do CTA

### Resultado
No painel do Vturb, usar o ID: **`cta-btn`**

